{
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "basic-ftp": "^5.0.3",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "nodemon dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
